/**
 * Imports styled
 */
import { styled } from "@mui/system";

/**
 * Styles the Pokecard
 */
export const TitleContainer = styled("h1")(() => {
  return {
    fontFamily: "Lucida Handwriting",
    color: "blue",
    fontSize: "33px",
  };
});
